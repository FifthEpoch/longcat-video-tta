# HOLD is not “N=32 is enough” (2026-08-28)

**Call:** The user is right. Long-horizon papers on this model family
cite **N ≈ 128**, not 32. HOLD never meant “skip the paper-size
table.” It meant “do not scale tonight, and do not scale NOs.”

---

## Field N (we already wrote this on 2026-08-18)

On Wan 1.3B + a Self-Forcing-style student, the copied long table is
**30–60 s, first 128 MovieGen prompts (often Qwen-refined),
VBench-Long on the full clip.**

Self-Forcing++ / Relax Forcing / FreqForcing / Self Gradient Forcing
/ Rolling Forcing sit on that recipe. Not every video paper uses 128
(History-Guided is Kinetics N=1024; short VBench is hundreds of
prompts). For *this* long-horizon cluster, **128 is the number.**

N=32 caption V2V is a **discovery / mechanism** set. Dyn% 7/32 vs
13/32 will not survive a reviewer who opens Relax Forcing.

---

## What HOLD actually reserved

| Arm | N now | Promote to 128? |
|---|---|---|
| Self Forcing | 32 | **Yes** — baseline at paper N |
| Pseudo-future Search | 32 | **Yes** — the cite method |
| Always-search | 32 | **Yes** — required ablation at the same N |
| Rewind | 32 | Optional cheap row; not the lead |
| Sick / Sink / Prefix / AdaSteer / RF intra / RF Pseudo / appear | 8 or 32 | **No** |
| lastmix / intra / bpseudo / restep | 8 in flight | Harvest first. Only a HOLD even gets a 32 talk |

The method note already said this: “Scale only Self Forcing /
Always-search / Pseudo on caption V2V. N=32 is the discovery set.”
(`2026-08-25_pseudo_future_search.md`)

---

## Two different 128s — do not collapse them

| Suite | Matches the field table? | Matches our claim? |
|---|---|---|
| **Caption V2V Panda N=128** | No standard V2V-128 leaderboard | **Yes** — visual prefix → long AR |
| **T2V MovieGen-128 + VBench-Long** | **Yes** — Relax / SF++ cell | Weaker — text start, not visual history |

Task lock stays V2V. T2V 128 is still the optional apples-to-apples
compare, not a replacement. Spec exists:
`2026-08-18_wan_t2v_vbenchlong_128_spec.md`.

---

## When — corrected 2026-08-28 03:43

GRES is **queue**, not a scientific hold. Caption N=32 notta was
~2 h 45; N=128 is one H200 overnight (~11–16 h). Lastmix stays
PD behind it. Do **not** scancel lastmix.

Submit **baselines tonight**:

```bash
cd /scratch/wc3013/longcat-video-tta && git pull --ff-only origin main
WAVE=hosts bash wan_experiment/sbatch/submit_v2v_caption128.sh
```

That is Self Forcing + Rolling Forcing do-nothing on caption
`metadata.csv`, first 128 of the same Panda pool. Series:
`v2v_panda_caption_128v`.

Pseudo + Always can ride the same queue (`WAVE=cite` after hosts,
or `WAVE=all`). They do not need lastmix harvest. NOs still stay
off this 128.

Optional later: T2V MovieGen-128 VBench-Long for the field cell.
