# Experimental Outputs — 2026-08-14

This file accumulates every cluster output the user pasted on this date,
plus 1-3 line interpretation of each. Raw blocks are preserved verbatim
so future agents can re-analyze.

---

## 17:28 — TTC w=0 smoke test (shard_0000, jobs 15699080–083): ENGINE RUNS, OUTPUT IS GARBAGE

**Source:** user paste of `lh_drift_15699080.out` + shard_0000 listing.
**Run:** `longhorizon_sweep_ttc_w0_native_12ch` (METHOD=ttc TTC_WEIGHT=0, native
13/80, 12 chunks, N=8, seed=42). First GPU execution of the TTC/SAViDNO path
inside `diag_longhorizon_drift.py`.

```
[1/2] 86KC2a8qcQI_seg21
  chunk  1: sharp=0.0021 motion=0.1309 colorful=0.2268 seam_ratio=2.53 psnr=7.38 corr=n(n=0)
  ... chunks 2-12: sharp=0.0021 motion≈0.130 colorful≈0.231 psnr=nan ...
[2/2] 9onqdz1EHcw_seg5
  chunk  1: sharp=0.0021 motion=0.1311 colorful=0.2351 seam_ratio=2.39 psnr=8.38 corr=n(n=0)
  ... chunks 2-12: same constants ...

Drift verdict over 2 videos x 12 chunks:
  sharpness       : 0.0021 -> 0.0021 (-1.2%)
  temporal_motion : 0.1310 -> 0.1300 (-0.8%)
  colorfulness    : 0.2309 -> 0.2311 (+0.1%)
  contrast        : 0.2230 -> 0.2210 (-0.9%)
  psnr            : 7.8808 -> 8.5352
  lpips           : 0.9392 -> 0.9035
Saved: .../shard_0000/summary.json   (no traceback)
```

**Interpretation:** The job completed (2 videos × 12 chunks, `summary.json`
written, stderr is only FutureWarnings) so the integration / joint decode /
frame-geometry path did not crash. The *content* is not a valid baseline.
Same two videos under native NOTTA / best-of-N had PSNR ~16–21 dB, sharpness
~0.006–0.010, motion ~0.012–0.023. Here PSNR is 7–8 dB, LPIPS ~0.94 (near
random), sharpness is stuck at 0.0021 and motion at ~0.130 on *both* a car
clip and a watch clip, flat across 12 chunks. That is the exact signature
SAViDNO already documented for the wrong Euler sign (`x_t += dt*v` steps
away from clean → decoded initial noise, PSNR ~9, identical regardless of
CFG/steps). `TTC_LongCat.sample` still had that old sign; SAViDNO's own
sampler was later flipped to `x_t -= dt*v` to match `generate_vc`.

**Action taken:** do NOT launch `ttc w0.1` / `ttc_gated`. Do NOT treat this
series as a paired baseline. Fix `ttc_longcat.py` Euler + x0 convention to
match SAViDNO/`generate_vc`, then resubmit w=0 as a smoke test. Remaining
15699081–083 shards (if still running) should be cancelled; their output is
the same garbage.

---

## 18:39 — TTC w=0 v2 resubmitted after Euler-sign fix (jobs 15762652–655)

**Source:** user paste — `scancel` of broken 1569908x + `git pull` to `38397a8` +
`SERIES=longhorizon_sweep_ttc_w0_native_12ch_v2 METHOD=ttc TTC_WEIGHT=0` submit.

```
Submitted 4 shard jobs: 15762652 15762653 15762654 15762655
series: longhorizon_sweep_ttc_w0_native_12ch_v2
ttc: sigma<=0.3 cadence=1 weight=0  (engine-native NOTTA baseline)
```

**Interpretation:** Correct isolation — new series so the garbage v1 shards cannot
be merged in. Still a smoke test, not a result. Pass on shard_0000 (same two
videos): chunk-1 PSNR ~16–20 dB and GT-free signals that differ across videos /
chunks. Fail: sharp=0.0021 / psnr=7.x again.

**Action taken:** logged. Hold `ttc w0.1` and `ttc_gated` until shard_0000 passes.

---
