# Experimental Outputs — 2026-07-15

This file accumulates every cluster output the user pasted on this date,
plus 1-3 line interpretation of each. Raw blocks are preserved verbatim
so future agents can re-analyze.

---

## 01:39 — Pilot config vs NOTTA vs routing, full metrics (FIRST RUN — ⚠️ SUPERSEDED)

**Source:** user paste / `analyze_pilot_config_vs_notta_full.py` first run (commit `274bd1d`)
**Run:** `per_video_analysis/2026-07-14/pilot_config_vs_notta_full.md`

> ⚠️ **SUPERSEDED — video-set bug.** This first run let the NOTTA baseline
> (full `panda_1000v_standard`, ~999 videos) into the analysis set, so
> `Videos (pilot union) = 999`. Config rows are over the 200 pilot videos but
> the **NOTTA population row (N=999) and all Task-2 quintile bins (200 each,
> ~999 total) cover a different, larger pool.** Paired Task-1b deltas are still
> valid (pairing intersects to the 200). Fixed in commit after `274bd1d`
> (`vids` restricted to grid-config coverage). DO NOT cite the NOTTA population
> row or the raw quintile means from this run.

```
Videos (pilot union): 999   [BUG: should be 200]
NOTTA row N=999; config rows N=200; quintile bins ~200 each.

Task 1 (population, PSNR / VBench-rawmean):
  NOTTA (N=999):              17.930 / 9.9450   [over 999, not comparable]
  fixed S10_LR5e3 (N=200):    17.996 / 9.3979
  ORACLE-PSNR-route (N=200):  18.744 / 9.4140
  ORACLE-VBench-route:        18.023 / 9.5382

Task 1b (paired over 200 — TRUSTWORTHY):
  ΔPSNR(oracle − NOTTA)      = +0.947 dB
  ΔPSNR(oracle − fixed S10)  = +0.748 dB
  ΔVBench(oracle − NOTTA)    = -0.4549 (raw-mean; NOTTA higher)
  every fixed config: ΔPSNR vs oracle +0.62..+0.86 dB

Task 2 PSNR by OOD quintile (NOTTA cells provisional):
  Q1 NOTTA 18.711 / oracle 19.161 (+0.450)
  Q2 NOTTA 19.510 / oracle 19.419 (-0.091)
  Q3 NOTTA 18.677 / oracle 20.171 (+1.494)
  Q4 NOTTA 17.052 / oracle 18.749 (+1.696)
  Q5 NOTTA 15.705 / oracle 16.221 (+0.517); best FIXED config 15.466 < NOTTA
```

**Interpretation (trustworthy parts only):**
1. **PSNR oracle routing beats no-TTA by ~+0.95 dB and fixed S10 by +0.748 dB**
   (paired, N=200) — sanity-matches the known pilot oracle gap. Every single
   fixed config is +0.62..+0.86 dB *below* oracle → routing's value is per-video
   config choice, not any one budget.
2. **OOD-tail failure of fixed TTA:** in Q5 (most OOD) the best fixed config
   (15.47) is *below* NOTTA — provisional, but suggests fixed AdaSteer can hurt
   the hardest videos while oracle routing still recovers a net gain.
3. **VBench column is a raw-dim mean** (dominated by imaging_quality/MUSIQ
   0–100), NOT normalized VBench++ (~0.77). NOTTA > TTA on this scale most
   likely reflects imaging_quality dropping under adaptation. Use the per-dim
   VBench oracle report for the perceptual story; do not put this VBench next to
   the slide's 0.772/0.802.

**Action taken:** Fixed analyzer (restrict `vids` to pilot grid coverage;
added VBench scale caveat). Corrected re-run pending; will log + build paper
table from the corrected output.

---

## 01:39 — OOD segment-pool scoring job status

**Source:** user paste / `squeue` + `wc -l`

```
       JOBID                           NAME    STATE       TIME
    13491658          compute_diffusion_ood  RUNNING   22:41:51
22937 sweep_experiment/reports/per_video_analysis/2026-07-10/diffusion_ood_scores_segment_pool.csv
```

**Interpretation:** OOD scoring still RUNNING at 22.7 h, ~22,936 / 29,578
target (~78%). Well past the ≥1000-scored threshold the preview sampler needs,
so a partial-pool 1000v preview can be fired now if desired; the paper-final
full-pool resample still needs the remaining ~6.6K.

---
