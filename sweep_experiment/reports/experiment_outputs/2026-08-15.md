# Experimental Outputs — 2026-08-15

This file accumulates every cluster output the user pasted on this date,
plus 1-3 line interpretation of each. Raw blocks are preserved verbatim
so future agents can re-analyze.

---

## 01:10 — Wan / Self-Forcing overnight setup chain submitted

**Source:** user paste — `git pull` to `5ea5d74` + `bash wan_experiment/sbatch/submit_setup_chain.sh`

```
[1/3] setup_env      job 15772007  (GPU, ~2h)
[2/3] download       job 15772008  (CPU, ~4h, parallel with 1)
[3/3] healthcheck    job 15772009  (GPU, afterok 15772007+15772008)
```

**Interpretation:** Chain is correctly wired. 15772007+008 run in parallel;
15772009 will not start unless both succeed. User can disconnect.

**Action taken:** logged. Next: when 15772009 finishes, read
`wan_experiment/results/setup_healthcheck/report.json`. Cancel if needed:
`scancel 15772007 15772008 15772009`.

---

## 14:29 — Setup chain gone; healthcheck never wrote a report

**Source:** user paste — `cat report.json` + `squeue` + `tail wan_healthcheck_15772009.out`

```
cat: .../setup_healthcheck/report.json: No such file or directory
squeue -u wc3013  -> empty
tail: cannot open '.../wan_healthcheck_15772009.out': No such file or directory
```

**Interpretation:** 15772009 never started. `afterok:15772007:15772008` cancelled
it because setup_env and/or download exited non-zero (or was cancelled/preempted
without a successful completion). Do not resubmit until sacct + the two job
logs identify the failing step.

**Action taken:** logged. Waiting on sacct + `wan_sf_env_15772007` /
`wan_sf_dl_15772008` logs.

---
