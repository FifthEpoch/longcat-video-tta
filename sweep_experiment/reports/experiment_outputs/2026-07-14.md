# Experimental Outputs — 2026-07-14

This file accumulates every cluster output the user pasted on this date,
plus 1-3 line interpretation of each. Raw blocks are preserved verbatim
so future agents can re-analyze.

---

## 11:24 — S10 preview resubmit all COMPLETED; OOD 13491658 still running

**Source:** user paste / sacct + squeue

```
13491234–13491253: all COMPLETED 0:0 (~4.1h each)
13491658: OOD segment-pool RESUME job RUNNING 8:25:45
```

**Interpretation:** S10 gate cleared for merge/audit. OOD full pool independent of preview router.

**Action taken:** merge → audit → router path in chat.

---

## 12:42 — Post-S10 merge + audit; PSNR router crash

**Source:** user paste / cluster

```
bash scripts/run_preview_1000v_pipeline.sh merge
bash scripts/run_preview_1000v_pipeline.sh audit
  (12 configs: ~997–998 finite PSNR each; population PSNR ~19.3 dB)
videos with finite PSNR in ALL present runs: 115
OK — ready for router eval.

bash scripts/run_deploy_psnr_router_preview.sh
ValueError: 'S10_LR5e3' is not in list
```

**Interpretation:** Two independent issues.
(1) **Router crash:** `load_pilot_bundle()` filtered the grid through `filter_vbench_grid_runs()`; preview sweep ran `COMPUTE_VBENCH=0` so `grid_runs` was empty → `grid.index('S10_LR5e3')` crashed. Pure code bug.
(2) **Intersection collapse (115):** per-video metrics are keyed by canonical *filename* id (`_canonical_video_id`), NOT positional index. Each config has ~998 finite, but the 12 configs scored *different physical video sets* → intersection 115. Root cause: the preview dataset's symlinks were still being materialized when the first sweep launched (same window that gave S10 its 354 `[Errno 2] No such file`). Configs launched at different moments saw different dataset snapshots. Per-config compute is individually valid but not on a common video set, so it can't back a per-video router.

**Correction:** earlier chat said "resubmit 120 jobs" — that was an unscoped worst case. S10_LR1e3/S10_LR5e3 already ran against the stable dataset and are fine. Actual rerun scope = only the configs whose video set diverges from the fresh reference; determined cheaply on CPU (no GPU) via new `scope` phase.

**Action taken:** Code fixes — (a) `require_vbench=False` PSNR-only bundle path in `budget_routing_common.py` + `run_deploy_psnr_router.py`; (b) audit now FAILS if intersection `<900`; (c) added `scripts/diagnose_preview_intersection.py` (+ `scope` phase) to report per-config/per-chunk overlap vs reference and emit an `ONLY_RUNS=...` targeted rerun command; (d) added dataset-stability guard to `submit_adasteer_budget_1000v_preview.sh` (aborts if `<MAX_VIDEOS` present or any broken symlink) so this cannot recur; (e) `wipe_preview_1000v_sweep.sh` + `resweep` phase. **Next on cluster:** `git pull`, run `scope`, then targeted `ONLY_RUNS` wipe+resubmit for only the misaligned configs.

---
