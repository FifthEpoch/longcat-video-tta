# Experimental Outputs — 2026-07-11

This file accumulates every cluster output the user pasted on this date,
plus 1-3 line interpretation of each. Raw blocks are preserved verbatim
so future agents can re-analyze.

---

## 10:47 — OOD job 13287176 cycle 2 progress + TIMEOUT (empty squeue)

**Source:** user paste / tail log + squeue  
**Run:** job `13287176` (resubmit after 13249677)

```
Log tail: [1150/28411] done=1150 @ 0.08 vid/s, ETA ~5520m remaining
squeue: empty (job finished)
Prior cycle 13249677: TIMEOUT @ 4h → 1166 rows in segment_pool CSV
This cycle: +~1150 vids → expect ~2316 total rows in CSV
```

**Interpretation:** Hit **4h walltime** again (same ~1150 vids/4h). Correct paths this time. Increase `#SBATCH --time` to 48:00:00; add `submit_segment_pool_ood.sh` wrapper to prevent env-var mistakes on resume.

**Action taken:** Walltime bump + submit wrapper in repo; user resubmit via wrapper after `git pull`.

---

## 10:49 — Cycle 2 TIMEOUT confirmed; job 13325919 submitted (48h)

**Source:** user paste / sacct + git pull + submit wrapper

```
13287176: TIMEOUT 04:00:21
CSV: 2337 lines (2336 videos, 7.9% of 29577)
git pull: f51c4d7..c4e0be8 (48h sbatch + submit_segment_pool_ood.sh)
13325919: submitted via wrapper, RESUME=1, correct paths
```

**Interpretation:** On track. At 48h walltime expect ~14K more videos/cycle → ~2 more cycles after this one to finish segment pool OOD.

**Action taken:** Monitor 13325919; target CSV line count 29578.

---

## 23:15 — Preview 1000v sampling proposal @ 5885 scored (job 13325919 R 12h)

**Source:** user paste / wc -l during OOD job  
**Run:** job `13325919` still running

```
CSV: 5886 lines (5885 scored, 19.9% of pool)
User: stratified sample 1000 from partial pool for router preview
```

**Interpretation:** Feasible — each quintile has ~1177 candidates >> 200 needed. Caveat: scored set is early **video_id-sorted prefix**, not random; quintile edges differ from final 29K run. Scripts added: `sample_segment_pool_ood_preview_1000v.sh`, `submit_adasteer_budget_1000v_preview.sh`.

**Action taken:** User runs sampling on cluster; parallel with ongoing OOD job OK.

---

## 11:21 — Preview 1000v router pipeline scripts added

**Source:** user request / implementation  
**Refs:** `run_preview_1000v_pipeline.sh`, `submit_preview_1000v_features.sh`, `submit_deploy_router_1000v_preview.sh`

Full preview path: sample → sweep → merge → features (filter OOD from segment CSV) → sweep-mp4 + vbench backfill → router CPU suite.

---
