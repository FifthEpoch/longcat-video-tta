# Experimental Outputs — 2026-08-16

This file accumulates every cluster output the user pasted on this date,
plus 1-3 line interpretation of each. Raw blocks are preserved verbatim
so future agents can re-analyze.

---

## 14:48 — Resubmitted CPU setup_env + healthcheck (flash-attn skipped)

**Source:** user paste — `git pull` to `6b14ed7` + sbatch env then healthcheck
**Run:** setup_env=15858268  healthcheck=15858269

```
Updating 987d99f..6b14ed7
setup_env=15858268  healthcheck=15858269
```

**Interpretation:** Pull landed the flash-attn skip + CPU setup. 15858269 waits
on 15858268 (`afterok`). 15858268 should start quickly (no GPU GRES). Do not
resubmit download. Disconnect is fine.

**Action taken:** logged. Next: when 15858269 finishes, read
`wan_experiment/results/setup_healthcheck/report.json`.

---

## 14:59 — Healthcheck GREEN (15858269); optional smoke_t2v failed on write_video

**Source:** user paste — `report.json` + healthcheck `.out`
**Run:** setup_env=15858268 + healthcheck=15858269 (2026-08-16 15:54 EDT)

```
{
  "ok": true,
  "n_fail_required": 0,
  "checks": [
    {"name": "wan_config", "ok": true},
    {"name": "wan_t5", "ok": true, "n": 2, "bytes": 22723840836},
    {"name": "wan_vae", "ok": true, "n": 1},
    {"name": "sf_ckpt", "ok": true, "bytes": 5676252553},
    {"name": "vbench_i2v_images", "ok": true, "n_found": 105, "n_decoded": 8, "required": false},
    {"name": "torch", "ok": true, "version": "2.13.0+cu130", "cuda": true, "gpu": "NVIDIA H200"},
    {"name": "sf_ckpt_load", "ok": true, "top_keys": ["generator_ema"], "n_tensors": 0},
    {"name": "wan_config_parse", "ok": true, "model_type": "t2v"},
    {"name": "smoke_t2v", "ok": false, "required": false, "rc": 1, "seconds": 16.6,
     "stderr_tail": "ImportError: cannot import name 'write_video' from 'torchvision.io'"}
  ]
}
All required checks passed.
```

**Interpretation:** Required stack is up: Wan 1.3B + T5 (~22.7 GB) + VAE + SF DMD
(5.3 GB, key `generator_ema`) + 105 VBench-I2V images + torch 2.13.0+cu130 on
H200. Official `inference.py` smoke is not a blocker; torchvision 2.13 dropped
`write_video`. Do not call that entry point as-is. Next: our own I2V /
prefix-conditioned continuation runner (imageio/av writer; unwrap `generator_ema`).

**Action taken:** logged. Setup chain closed.

---

## 15:01 — Started I2V continuation runner + 2×5 s NOTTA smoke

**Source:** user "let's start"
**Run:** not submitted yet — waiting on cluster pull + `submit_i2v_smoke.sh`

**Interpretation:** Healthcheck is green. First gated job is 2 VBench-I2V
images at 5 s (series `i2v_notta_smoke`), not the 16×30 s suite.

**Action taken:** added `run_i2v_continuation.py` + `run_i2v_notta.sbatch` +
`submit_i2v_smoke.sh`. User submits after pull.

---

## 15:12 — I2V NOTTA smoke submitted (job 15858704)

**Source:** user paste — `git pull` to `d841a43` + `submit_i2v_smoke.sh`
**Run:** 15858704  series=`i2v_notta_smoke`  5s n=2

```
Updating 6b14ed7..d841a43
i2v_notta_smoke 5s n=2  job 15858704
```

**Interpretation:** Smoke is in. Do not submit 16×30 s until summary.json
shows n_ok==n and mp4s look like video.

**Action taken:** logged.

---

## 20:18 — I2V smoke 15858704 FAILED: flash-attn assert

**Source:** user paste — `summary.json` + `ls` of `i2v_notta_smoke/h5s_shard0`
**Run:** 15858704  n=2  n_ok=0

```
"error": "AssertionError: "
wan/modules/attention.py", line 118, in flash_attention
    assert FLASH_ATTN_2_AVAILABLE
```

Both clips failed (3.18s / 0.13s). No mp4s. Pipeline reached the first
DiT forward — load + I2V encode path is fine.

**Interpretation:** Skipping flash-attn in setup_env was correct for the
2h compile TIMEOUT, but Self-Forcing `model.py` calls `flash_attention()`
directly (the SDPA fallback lives only on `attention()`). Patch the
runner to use PyTorch SDPA when flash-attn is missing. Do not recompile
flash-attn.

**Action taken:** SDPA fallback in `run_i2v_continuation.py`. Resubmit
the same 2×5 s smoke after pull.

---

## 20:22 — Resubmitted I2V smoke with SDPA fallback (job 15876397)

**Source:** user paste — `git pull` to `21531d9` + `submit_i2v_smoke.sh` + squeue
**Run:** 15876397  series=`i2v_notta_smoke`  5s n=2

```
Updating d841a43..21531d9
i2v_notta_smoke 5s n=2  job 15876397
15876397 h200_cour wan_i2v_   wc3013 PD  (ReqNodeNotAvail, May be reserved for other job)
```

**Interpretation:** Pull landed the SDPA fallback. Job is pending on GPU
availability, not a script error. Failed jsons from 15858704 have no mp4s,
so the runner will retry both clips.

**Action taken:** logged. Next: `summary.json` when 15876397 finishes.

---

## 21:16 — I2V smoke 15876397 OOM: 138 GB KV cache

**Source:** user paste — `summary.json` after 15876397
**Run:** 15876397  n=2  n_ok=0

```
OutOfMemoryError: CUDA out of memory. Tried to allocate 56.00 MiB.
GPU 0 has a total capacity of 139.80 GiB of which 23.19 MiB is free.
... 138.10 GiB is allocated by PyTorch
```

First clip OOM in `causal_rope_apply` (3.96s); second OOM in VAE encode
(0.18s) because the first alloc was not freed.

**Interpretation:** SDPA fallback worked (we reached DiT self-attn). The
138 GB footprint matches `24 frames × 32760 tokens × 30 blocks × K+V ×
12 × 128 × bf16`, i.e. treating `pipeline.frame_seq_length` as 32760
(the *max seq*, 21×1560) instead of 1560 tokens/frame. Official 5 s
cache is 21×1560=32760 tokens (~7 GB). Hardcode 1560 and cap estimate
at 48 GB.

**Action taken:** fix `enlarge_kv_cache`. Resubmit same 2×5 s smoke.

---

## 21:42 — Resubmitted I2V smoke after KV-cache fix (job 15877786)

**Source:** user paste — `git pull` to `dbbf533` + `submit_i2v_smoke.sh` + squeue
**Run:** 15877786  series=`i2v_notta_smoke`  5s n=2

```
Updating 21531d9..dbbf533
i2v_notta_smoke 5s n=2  job 15877786
squeue -u wc3013  -> empty
```

**Interpretation:** Pull landed the 1560-token/frame fix. Empty squeue
right after submit means the job already left the queue (finished, failed,
or rejected) — not still pending. Check sacct + summary.json next.

**Action taken:** logged.

---

## 21:44 — I2V smoke 15877786 still OOM at 138 GB (KV fix held)

**Source:** user paste — sacct + summary + log tail
**Run:** 15877786 FAILED 2:0 in 2m29s  n_ok=0

```
138.10 GiB is allocated by PyTorch  (same footprint as 15876397)
cuda_mem[after_fail] allocated=148.28G reserved=149.35G
OOM in causal_rope_apply (denoise loop, line 197) then VAE encode
```

**Interpretation:** The 48 GB KV cap did not fire, so our cache was not
the 135 GB alloc. Job reached the denoise loop (further than 15876397).
Identical 138.10 GB fill points at `torch.compile(flex_attention,
max-autotune)` at Self-Forcing import time on a 140 GB H200. Disable
compile + eager flex_attention + T5 DynamicSwap + low_memory=True.

**Action taken:** patched runner (`14eb0bd`). Resubmit same 2×5 s smoke.
If it fails again, paste `head -n 40` of the .out (KV + cuda_mem lines).

---

## 21:57 — Resubmitted I2V smoke with compile disabled (job 15879723)

**Source:** user paste — `git pull` to `c9289ad` + `submit_i2v_smoke.sh` + squeue
**Run:** 15879723  series=`i2v_notta_smoke`  5s n=2

```
Updating dbbf533..c9289ad
i2v_notta_smoke 5s n=2  job 15879723
15879723 h200_cour wan_i2v_   wc3013 PD  (Priority)
```

**Interpretation:** Pull landed compile-disable + T5 swap. Job is queued
(Priority), not running yet. Disconnect is fine.

**Action taken:** logged.

---

## 22:27 — I2V smoke 15879723 still 138 GB OOM (compile-disable did not help)

**Source:** user paste — summary.json after 15879723
**Run:** 15879723  n_ok=0  first clip 5.45s (OOM in qkv RMSNorm)

```
138.02 GiB allocated / 139.80 GiB  (same family as 15876397 / 15877786)
```

**Interpretation:** Disabling torch.compile did not change the 138 GB
fill. Do not resubmit the smoke. Need `head -n 50` of the .out and a
load-only VRAM probe.

**Action taken:** added `probe_vram.py`. No more blind smokes until we
know what the 138 GB tensor is.

---

## 22:30 — Log head of 15879723: KV cache is fine; grads were on

**Source:** user paste — `head -n 50 wan_i2v_notta_15879723.out`

```
cuda_mem[after_import] allocated=0.00G
KV cache: 24 frames x 1560 tokens ... est 6.90 GB
cuda_mem[after_load] allocated=3.09G
cuda_mem[before_inference] allocated=3.10G
Moving DynamicSwap_WanTextEncoder to cuda:0 with preserved memory: 141.2 GB
cuda_mem[after_kv_init] allocated=10.06G
current_timestep: 1000 / 937.5 / 833 / 625   (x2 blocks)
then OOM 138 GB
```

**Interpretation:** Load + KV cache are healthy (~10 GB). Memory explodes
across the first two denoise blocks. Official `inference.py` sets
`torch.set_grad_enabled(False)`; we did not. Wrapper always pads to
`seq_len=32760`, so the autograd graph of a few 30-block forwards fills
the H200. Probe is optional now.

**Action taken:** `set_grad_enabled(False)` + `inference_mode()` in the
runner. Resubmit the 2×5 s smoke.

---

## 22:59 — Autograd-off smoke submitted (job 15880611), then left queue

**Source:** user paste — `git pull` to `65ba50c` + `submit_i2v_smoke.sh` + squeue
**Run:** 15880611  series=`i2v_notta_smoke`  5s n=2

```
Updating 046e55e..65ba50c
i2v_notta_smoke 5s n=2  job 15880611
15880611 h200_cour wan_i2v_   wc3013 PD  (QOSGrpGRES)
[later squeue empty]
```

**Interpretation:** Pull landed the grad-disable fix. Job queued behind
the 2-way H200 GRES cap, then left the queue. That is either a finished
run (pass or OOM) or a never-started drop. Need `sacct` + `summary.json`
+ slurm log tail — do not guess.

**Action taken:** logged. Waiting on result paste.

---

## 23:00 — I2V smoke 15880611 PASSED (n_ok=2, real mp4s)

**Source:** user paste — sacct + summary.json + ls + slurm tail
**Run:** 15880611  series=`i2v_notta_smoke`  node=gh118  Elapsed=00:02:55

```
15880611      COMPLETED      0:0   00:02:55  2026-08-16T23:53:13  2026-08-16T23:56:08  gh118
n=2 n_ok=2  horizon=5.0  n_gen=21  n_pix=85  480x832
000 ...bubbles...   11.99s  5911952 B  T=85
001 ...vegetables... 8.01s  3923497 B  T=85
```

**Interpretation:** Autograd-off was the 138 GB fix. Generate is ~8–12 s
per 5 s clip after load (job wall 2:55 includes ~77 s load). This is the
first working Wan I2V continuation. Visual check of one mp4 still needed
(first frame vs cond image) before treating clips as content-valid.

**Action taken:** INDEX + ANALYSIS_LOG + timing table. Added
`submit_i2v_notta16.sh` for 16×{5,30}s. Do not launch LongCat TTC.

---
