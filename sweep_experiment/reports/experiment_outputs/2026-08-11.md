# Experimental Outputs — 2026-08-11

This file accumulates every cluster output the user pasted on this date,
plus 1-3 line interpretation of each. Raw blocks are preserved verbatim
so future agents can re-analyze.

---

## 13:00 — best-of-N (k=4) PARTIAL N=2 smoke test (shards 0000+0001 of 8)

**Source:** user pasted the full `merged_partial_n2.json` (partial merge of the two
finished shards while 6 shards still run). Series
`sweep_experiment/results/longhorizon_sweep_bestof_k4_native_12ch/`, native 13/80,
12 chunks, k=4, seed=42. Full raw JSON lives at the cluster path
`.../longhorizon_sweep_bestof_k4_native_12ch/merged_partial_n2.json` (+ per-shard
`shard_000{0,1}/summary.json`); not dumped inline (1000+ float lines) — it is
regenerable from the shards.

**Merged drift verdict (N=2, chunk1→last):**
```
sharpness        0.0098 -> 0.0078  (-20.2%)
temporal_motion  0.0124 -> 0.0131  (+6.0%)
colorfulness     0.1807 -> 0.1223  (-32.3%)
contrast         0.2837 -> 0.2209  (-22.1%)
psnr            21.00  -> 17.34    (-17.5%, only 2 GT chunks)
ssim             0.763 ->  0.700   (-8.3%,  only 2 GT chunks)
lpips            0.126 ->  0.219   (+74.5%, only 2 GT chunks)
```

**Chosen candidate per chunk (0 = NOTTA seed; strict-superset design):**
```
video 86KC2a8qcQI_seg21 (car): [3,3,1,0,2,2,0,1,0,1,1,3]  -> NOTTA won 3/12
video 9onqdz1EHcw_seg5 (watch): [1,0,1,2,0,2,0,3,2,1,1,3]  -> NOTTA won 3/12
Search-active (chosen != 0): 18/24 chunks (75%). Verifier is NOT a no-op.
```

**TRUE-quality check on the 3 chunks that still had GT (the credibility test):**
| chunk | NOTTA(cand0) PSNR / LPIPS | chosen cand | chosen PSNR / LPIPS | PSNR-oracle cand | outcome |
|---|---|---|---|---|---|
| v1 c1 | 21.06 / 0.136 | 3 | **22.09 / 0.109** | 3 | verifier beat NOTTA on BOTH; hit PSNR oracle |
| v2 c1 | 15.70 / 0.232 | 1 | **19.92 / 0.142** | 1 | verifier beat NOTTA by +4.2 dB; hit PSNR oracle |
| v2 c2 | 17.34 / 0.219 | 0 (=NOTTA) | 17.34 / 0.219 | 2 (17.41) | tied NOTTA; missed oracle by 0.07 dB (negligible) |

**Interpretation:** SMOKE TEST PASSED. (1) Schema is correct — 4 candidates/chunk
logged, each with the 4 verifier signals + seam + PSNR/SSIM/LPIPS, `chosen`/`score`
present, GT NaN where the source clip ran out, seeds = base + c·100003 as designed.
(2) The verifier actively selects (75% non-NOTTA) and, because cand0 = NOTTA seed,
the arm is a strict superset. (3) ENCOURAGING but tiny-N signal: on all 3 GT chunks
the GT-free pick was ≥ NOTTA on PSNR (2 strict wins incl. +4.2 dB, 1 tie) and hit
the PSNR oracle 2/3 — i.e. the GT-free verifier's choice aligned with true quality.
STRONG CAVEAT: N=2 videos / 3 GT chunks = anecdote, not evidence. Panda preview
clips are short so GT covers only chunks 1-2; the real evidence at N=8 will be the
GT-free drift-vs-NOTTA paired sign-flip test + the random-pick/oracle noise-floor
gate, not the sparse GT.

**Action taken:** validated analyzer input contract by inspection + hand-computation;
`gen_time ≈ 26,650 s/video (~7.4 h)` confirms per-video cost (k=4 × 12 chunks native).
Full N=8 ≈ several more hours. NEXT: at N=8, merge all shards -> run
`analyze_bestof_search.py` (verifier vs random vs oracle) + `compare_drift_paired.py`
vs `longhorizon_sweep_notta_native_12ch`.

### De-risk: ran the (previously untested) analyzer on the real values

Built `_cluster_pull/fixture_bestof_n2.json` from the exact candidate values above
and ran `scripts/analyze_bestof_search.py` locally (matplotlib 3.10.7 env). It runs
clean end-to-end (all 4 sections, saves `search_analysis.json`) — **the analyzer is
now de-risked before N=8.** Section 3 (TRUE-quality on GT chunks) uses the COMPLETE
GT set (only 3 GT-overlapping chunks exist across both videos), so it is faithful:
```
PSNR   cand0=18.03  random=18.79  chosen=19.78  oracle=19.81
       chosen-cand0 = +1.75 dB   chosen-random = +0.99 dB (GATE: >0 => real signal)
       oracle-random = +1.02 dB (noise floor)   chosen beats cand0 in 2/3
LPIPS  cand0=0.196  random=0.178  chosen=0.157  oracle=0.157
```
So on the complete (tiny) GT set the GT-free verifier's pick beat BOTH cand0 (NOTTA)
AND the random-pick noise floor, and nearly captured the PSNR oracle — the exact
pattern we want. (Section 2 composite in the fixture used only 4 chunks so its
absolute value is not the full-24-chunk number; search-activity 75% matches the full
data.) HARD CAVEAT: N=2 / 3 GT chunks = anecdote. The verdict is the N=8 paired
sign-flip drift test + random/oracle gate. Also note GT coverage is ~1-2 chunks/video
(short Panda clips), so at N=8 the GT-based verifier check stays thin — the primary
evidence is the GT-free drift-vs-NOTTA paired test.

---
