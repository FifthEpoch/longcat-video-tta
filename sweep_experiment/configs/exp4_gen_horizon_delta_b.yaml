# Series 27: Experiment 4 -- Generation Horizon Ablation (Delta-B)
# Goal: Vary generation horizon to study impact on adaptation quality.
# Fixed: Delta-B best config (delta_lr=1e-2, delta_steps=20, num_groups=1), num_cond_frames=14
# Swept: num_frames

method: delta_b
series: 27
series_name: exp4_gen_horizon_delta_b
description: "Generation horizon ablation: Delta-B TTA"

fixed:
  delta_lr: 1.0e-2
  delta_steps: 20
  num_groups: 1
  num_cond_frames: 14
  gen_start_frame: 32
  num_inference_steps: 50
  guidance_scale: 4.0
  resolution: 480p
  seed: 42
  max_videos: 100

sweep:
  - run_id: GH_DB1
    num_frames: 16
  - run_id: GH_DB2
    num_frames: 28
  - run_id: GH_DB3
    num_frames: 44
  - run_id: GH_DB4
    num_frames: 72
