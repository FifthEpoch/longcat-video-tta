# Series 10: Delta-B -- LR Sweep (at best group count)
# Goal: Refine LR for the best group count from Series 9.
# Fixed: num_groups=BEST_FROM_S9, steps=20
# Swept: delta_lr
# NOTE: Update num_groups after Series 9 completes.

method: delta_b
series: 10
series_name: delta_b_lr_sweep
description: "Delta-B LR sweep at best group count"
depends_on: [9]

fixed:
  num_groups: 4      # PLACEHOLDER -- update from Series 9 best
  delta_steps: 20
  num_cond_frames: 14
  num_frames: 28
  gen_start_frame: 32
  num_inference_steps: 50
  guidance_scale: 4.0
  resolution: 480p
  seed: 42
  max_videos: 100

sweep:
  - run_id: DB7
    delta_lr: 1.0e-3
  - run_id: DB8
    delta_lr: 5.0e-3
  - run_id: DB9
    delta_lr: 1.0e-2
  - run_id: DB10
    delta_lr: 5.0e-2
  - run_id: DB11
    delta_lr: 1.0e-1
