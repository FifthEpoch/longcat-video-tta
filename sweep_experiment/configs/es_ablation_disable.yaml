# Series 13: Early Stopping Ablation -- Disable Early Stopping
# Goal: Baseline comparison with early stopping disabled for Full-model TTA.
# Fixed: Full-model best config (lr=1e-5, steps=20, warmup=3, optimizer=sgd)
# Swept: es_disable

method: full
series: 13
series_name: es_ablation_disable
description: "Early stopping ablation: disable early stopping baseline"

fixed:
  learning_rate: 1.0e-5
  num_steps: 20
  warmup_steps: 3
  weight_decay: 0.01
  max_grad_norm: 1.0
  optimizer: sgd
  num_cond_frames: 14
  num_frames: 28
  gen_start_frame: 32
  num_inference_steps: 50
  guidance_scale: 4.0
  resolution: 480p
  seed: 42
  max_videos: 100

sweep:
  - run_id: ES_D1
    es_disable: true
