# Local-only analysis archives (cluster dumps pasted to laptop agent).
# Committed summaries live in experiment_outputs/ and ANALYSIS_LOG.md.
sweep_experiment/reports/local_archive/**/*
!sweep_experiment/reports/local_archive/README.md
